# Local agent instructions (first-use document, not versioned yet)
/AGENTS.md

# Windows alternate data stream metadata materialized by WSL/copies
*:Zone.Identifier

# Local configuration that may contain machine-specific values or credentials
.env
.env.*
!.env.example

# Generated simulations, reports and implementation runs
/results/*
!/results/.gitkeep
